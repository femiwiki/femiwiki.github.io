# AGENTS.md

문서가 어떻게 만들어지는지는 [wikven 문서](https://chaotic-ground.github.io/wikven/Pages)에
있습니다. 여기에는 저장소의 다른 파일이 말하지 않는 것만 적습니다.

## 글쓰기

한국어로, 고치는 문서의 목소리를 따릅니다. "테라폼", "워커"로 적고, 사이트를 쓰는 사람은
"이용자"로 적습니다. 뒤에 올 내용을 요약하는 문단으로 열지 말고, 격언으로 닫지 마세요.

각주는 구두점 뒤에 붙입니다. `...끝났습니다.<ref>URL</ref>`이지
`...끝났습니다<ref>URL</ref>.`가 아닙니다. 커밋하기 전에 `grep '</ref>[.,]'`로 아무것도 나오지
않아야 합니다.

기계가 읽는 문자열은 `<code>`로 감쌉니다. 네트워크 표기(`<code>/24</code>`,
`<code>154.197.16.0/20</code>`), 파일 이름, 설정 키가 그렇습니다. 같은 것을 가리키는 보통 말은
그대로 둡니다.

근거는 1년 뒤에도 읽히도록 겁니다. 코드 한 줄에는 커밋에 고정된 퍼머링크를, 변경에는 PR이나
이슈 링크를 씁니다. GitHub Actions 실행 링크는 만료되니 걸지 마세요. 시크릿 값은 적지 않습니다.
식별자와 공개 주소, 계정 ID는 괜찮습니다.

수집기가 가져오는 숫자는 `main`에 적지 마세요. 겹칠 때 이기는 쪽은 `main`이라, 손으로 적은 값이
수집기가 채운 값을 가려 버립니다.

## 변경 보내기

한 PR은 한 커밋, 한 가지 일입니다. 커밋 메시지와 PR 제목, 본문은 한국어이고 본문은 500자
이하이며, 티켓 내용을 옮겨 적는 대신 링크를 겁니다.

머지하면 사람의 확인 없이 사이트에 나갑니다. 깨진 문서도 그대로 공개된다는 뜻입니다.
